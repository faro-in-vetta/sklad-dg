-- =====================================================================
-- DG FILATI · склад — коментар до оплати
-- Запускати у SQL Editor. Виконується швидко, дані не чіпає.
-- =====================================================================

alter table payments add column if not exists comment text;
comment on column payments.comment is 'Нотатка бухгалтера до платежу: номер переказу, банк, уточнення';

select 'готово' as "—";
