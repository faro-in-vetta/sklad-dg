-- =====================================================================
-- DG FILATI · Складський облік пряжі
-- Крок 9: нова роль «Клієнт» — ЧАСТИНА 1 з 2
--
-- УВАГА: цей файл виконується ОКРЕМО і ПЕРШИМ.
-- Postgres не дозволяє додати значення до типу й одразу ним скористатися
-- в тій самій транзакції, тому решта змін винесена в 10_client_rules.sql.
--
-- Порядок: спершу весь цей файл → потім 10_client_rules.sql.
-- Файл можна виконувати повторно.
-- =====================================================================

alter type user_role add value if not exists 'client';
